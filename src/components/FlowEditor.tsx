export function FlowEditor() {
  return (
    <div className="flex-1 bg-muted/30 p-3 font-mono text-xs text-muted-foreground">
      Editor placeholder — agent will mount Monaco here
    </div>
  );
}
