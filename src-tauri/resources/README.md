# Bundled resources

This directory holds files bundled inside the Tauri app.

- `scrcpy-server-v3.3.4.jar` — pushed to the Android device to start the screen mirror.
  Downloaded by the build script if missing. See `scripts/download-scrcpy.sh`.
